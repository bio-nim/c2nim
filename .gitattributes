# https://help.github.com/en/articles/dealing-with-line-endings

# Set the default behavior, in case people don't have core.autocrlf set.
# Git will always convert line endings to LF on checkout.
# You should use this for files that must keep LF endings, even on Windows.
* text eol=lf

# Declare files that will always have CRLF line endings on checkout.
testsuite/tests/matrix.cpp    binary
testsuite/tests/smartptrs.cpp binary
